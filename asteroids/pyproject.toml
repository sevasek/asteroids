[project]
name = "asteroids"
version = "0.1.0"
description = "A classic Asteroids arcade game built with Pygame"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
